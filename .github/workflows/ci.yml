name: Test

on:
  - workflow_dispatch
  - push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: swift-actions/setup-swift@v2
        with:
          swift-version: 6
      - uses: actions/checkout@v4
      - name: Build and Test
        run: swift test
